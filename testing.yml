---
- hosts: localhost
  connection: local
  gather_facts: no

  tasks:
   - name: Check hostname
     ansible.builtin.command: df -h
     register: result

   - name: Print result
     ansible.builtin.debug:
       var: result
